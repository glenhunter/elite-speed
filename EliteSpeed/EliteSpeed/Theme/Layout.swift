import SwiftUI

enum Layout {
    /// The one visible gap used between panels, keylines and text on the dashboard.
    static let gap: CGFloat = 12
    /// Screen edge to the roundel's side, so the number sits like it's centred on the bonnet.
    static let roundelSideMargin: CGFloat = 36
}
